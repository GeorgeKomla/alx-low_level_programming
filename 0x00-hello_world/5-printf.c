#include <stdio.h>

/**
 * main - entry point of program.
 *
 * Return: 0 at all times when there are no errors, return non zero when errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);

}
