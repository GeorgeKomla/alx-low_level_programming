#include <stdio.h>

/**
 * main - program to display 0-9 comma separated
 *
 * Return: 0 means success
**/
int main(void)
{
	int a;
	int b;
	
	for (a = 77; a <=89; a++)
	{
		for (b = a+1; b < 90; b++)
                {
			if ( a != 88 || b != 89);
			putchar(a);
			putchar(b);
			{
				putchar(',');
				putchar(' 'i);
                        }
										                }
        }
	putchar('\n');
        return (0);
}
